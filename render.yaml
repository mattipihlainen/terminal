services:
  - type: web
    name: terminal-in32
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /health
    envVars:
      - key: ICLOUD_CALENDAR_URL
        sync: false
      - key: ICLOUD_CALENDAR_URL_2
        sync: false
